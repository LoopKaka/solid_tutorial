class NotificationService {
    sendNotification(mode) {
        if (mode === "sms") {
            // TODO for SMS mode
        } else if (mode === "email") {
            // TODO for Email mode
        }
    }
}

module.exports = NotificationService;
